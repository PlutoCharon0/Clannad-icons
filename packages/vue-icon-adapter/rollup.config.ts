// rollup.config.js    "build": "pnpm supplement && rollup -c rollup.config.ts",
// @rollup/plugin-terser @rollup/plugin-typescript 
/* import typescript from '@rollup/plugin-typescript'
import { defineConfig } from 'rollup'
import terser from '@rollup/plugin-terser'
export default defineConfig({
	input: {
		'component': 'lib/component.ts',
		'index': 'lib/index.ts',
		'plugin': 'lib/plugin.ts',
		'vue-component-lib/utils': 'lib/vue-component-lib/utils.ts',
	},
	output: {
		dir: 'dist',
		sourcemap: true,
	},
	plugins: [
		typescript({
			tsconfig: './tsconfig.json',
			outDir: 'dist',
		}),
		terser(),
	],
}) */
